<nav class="navbar navbar-expand-lg fixed-top">
  <div class="container">
    <a class="navbar-brand fw-800 fs-4" href="/">REN</a>
    
    <button class="navbar-toggler border-0 btn-close-white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
        <li class="nav-item">
          <a class="nav-link ren-mono small" href="/sessions">Activity_Log</a>
        </li>
        <li class="nav-item">
          <a class="nav-link ren-mono small" href="/dashboard">Terminal.Main</a>
        </li>
      </ul>

      <div class="d-flex align-items-center gap-3">

        <% if (currentUser) { %>
          <div class="d-flex flex-column align-items-end me-2">
            <span class="ren-meta " style="font-size: 0.6rem;">STATUS: ONLINE</span>
            <span class="ren-mono small fw-600">
              <%= currentUser.username %>
            </span>
          </div>
          <a href="/auth/logout" class="btn btn-sm btn-outline-danger ren-mono px-3" style="font-size: 0.7rem;">
            TERMINATE_SESSION
          </a>

        <% } else { %>
          <button class="nav-link ren-mono small border-0 bg-transparent p-0 me-2" data-bs-toggle="modal" data-bs-target="#loginModal">
            SIGN_IN
          </button>
          
          <button class="btn btn-sm btn-solid ren-mono px-4 shadow-sm" data-bs-toggle="modal" data-bs-target="#registerModal">
            INITIALIZE
          </button>
        <% } %>

      </div>
    </div>
  </div>
</nav>